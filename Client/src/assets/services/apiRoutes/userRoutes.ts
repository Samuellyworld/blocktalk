import { hostRoute } from "../routesHelper";

const userRoute = `${hostRoute}/user`;

export const createUser = `${userRoute}/create`;
export const fetchAllUsers = `${userRoute}/getAllUsers`;
export const getUserByUserId = `${userRoute}`;


